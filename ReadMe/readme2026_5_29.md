# week1 Day3:Linux LOG-- Using namedpipe to achieve the function of Log
> **Date:** 2026/5/29
> **Environment:** Ubuntu Linux (Remote SSH) via VS Code (English UI)
> **Refrence** Linux Onlie Class 32

--- 

## ⭐ Today's Goals
- 2025/5/29
- [x] Achieve the function of log to record the messages happened in file by using namedpipe
### Log's function:
- [x] Support log level
- [x] Support multiple processes
- [x] Using FIFO(named pipes)
- [x] Background asynchronous log writing
- [x] Logs are written to the .log file

---

## 🍫 Implementation ideas
### 1. Create FIFO(once)
### 2. Create log process
### 3. Parent process' task(Producer) : business logic, generate logs, determine log level, wtrte to FIFO
### 4. Child process' task(Consumer) : read FIFO, parse log, write to file by category
### 5. Using macros to get line numbers and filenames


---

## ⭐ Core Concepts & Core Snippets

### 1.Function:
      mkfifo(const char *pathname, mode_t mode)
      pathname -- relative path/absolute path mode -- file permissions
      strtok(char *str, const char *delim)
      str -- the string to be processed delim -- characters/strings used as conditional statements


## ⭐ Mistakes & Debugging Lops

### 1. A loop statement was printed twice
       Reason: Buffer not flushed
### 2. FIFO only need to be created once and can be used long term
### 3. Get FIFO's fd by using the open function
### 4. Using the strcmp function to compare wether strings are equal
### 5. Using strlen function instead sizeof function


## ❌ Ideological misunderstandings

### 1. Log's funciton doesn't need the procress pool, because the procress pool is suitable for lots of indenpendent tasks
So, my coding level is not suitable for achieving the project about using the combination of process pools and named pipes(/(ㄒoㄒ)/~~) 