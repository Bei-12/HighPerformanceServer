#include "Thread_P_C.hpp"

int main()
{
    // BlockingQueue bq;
    // Thread c1(1, &bq), c2(2, &bq), c3(3, &bq);
    // c1.Start();
    // c2.Start();
    // c3.Start();
    // for(int i = 0; i < 100; ++i)
    // {
    //     bq.Push(Task(i));
    // }
    // bq.Push(Task(-1));
    // bq.Push(Task(-1));
    // bq.Push(Task(-1));
    // c1.Join();
    // c2.Join();
    // c3.Join();
    // return 0;
}